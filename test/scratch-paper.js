/**
 * The `fullName` function iterates through a person object with the properties
 * 'firstName` and `lastName` using forEach and returns full names. 
 * @param {String[]}  firstName Is an array of words
 * @param {String[]} lastName Is an array of words
 * @return {String[]} A new array from firstName and lastName 
 */

describe('fullNames', function() {
  it('makes full names', function(){
  console.log(fullNames)
  });
});

var people = ["kali kiger", "whit young"];

var fullName = function() {
  console.log("Their full name is" + firstName + lastName)
};
fullName(people)




